---
id: benchpack/wombat-snapshot
name: wombat-snapshot
description: Create a wombat-snapshot checkpoint of workspace state before risky edits
intent: checkpoint workspace state
tags: [wombat, snapshot, checkpoint, benchmark]
version: 1.0.0
namespace: benchpack
type: prompt
---

# wombat-snapshot

Benchmark fixture skill (gate45 P1). Deliberately distinctive tokens so the
keyword layer routes deterministically inside the pinned hermetic universe.
