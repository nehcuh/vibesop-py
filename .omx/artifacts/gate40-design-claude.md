## Verdict
PASS_WITH_NITS

证据核查（≥8 处全部核实）：`_layers.py:403`、`triage_recall.py:123`（MODEL_NAME=:38）、`evaluator.py:201-204`、`feedback_loop.py:143-148`/`:178`（F_MIN_ROUTES=3/:60）、`agent_runtime.py:557-563`/`:668`/`:692`、`cli/main.py:903-914`、`server.py:137/:195/:296/:330/:344`、`manager.py:553-563`（全仓零消费方）、`tool_sequences.py:137-154`、`models.py:791-795`、`gold_detection.py:108-144`、grok_build.py:188（`vibe route --hook` + timeout 10s）。gate38 §5 记档 3/4/5 与 gate39 §4 记档 1/2/3/4/6 同 gate40 五项一一对应，连续性成立；0d5f9d4 时间戳（08-21T06:03Z）精确。本仓 spans 无 `"skill_id": "fallback-llm"` 症状行、无 feedback/execution_feedback 燃料文件，均与稿中自述一致。

## Findings
- [MAJOR] 主项：§1.1 自设的风险要求“offline 失败→在线重试”在 §1.2 设计中丢失，且 env-var 路线在进程内不可实现（HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE 在 huggingface_hub import 时冻结，无法中途翻转重试）——唯一防线只剩“目录存在该模型快照”的目录级检测，partial snapshot / 自定义 HF_HOME 缓存位置会误判 → fail-open 静默降级为 keyword-only。embedding 是中文 query 的唯一有效层（BM25 中文 0% 召回），静默丢失 = 路由质量塌陷且无观测面。另：全仓 `SentenceTransformer(` 有 **6 处**加载点（strategies.py:599 / learner.py:695 / triage_recall.py:123 / promote_verifier.py:136 / _layers.py:403 / indexer.py:480），稿中只点名 2 处；且“bash 模板导出 + CLI 入口导出”方案下，已部署 hook（cmspark 等）须逐项目重跑 `vibe build` 才生效。建议改用 Python 侧 os.environ choke point（lazy `from sentence_transformers import` 之前、模块级单次）：“在 Python 里设环境变量”仍是环境变量方案，一处改动覆盖 hook/CLI/grok（grok_build.py:188 直呼 `vibe route`，timeout 10s，在线等待 13-30s 下该 hook 现状必超时——这也是主项紧迫性的额外佐证）+ 全部 6 加载点 + 随包升级自动 rollout，且缓存检测可用 huggingface_hub 原生解析而非 bash 重复 HF_HOME 逻辑。“不侵入 Python 代码”的偏好理由是伪二分。
- [MAJOR] 项 2：≥3 闸不治愈 Lane A 自己暴露的荒诞类。实证根因是 usage_frequency 按 max 归一（0.15 权重）+ execution feedback 缺位时 user_satisfaction/execution_success 双 0（evaluator.py:73-79；本仓无 .vibe/execution_feedback.json）——完美 accuracy 无 exec 反馈的质量上限 = 0.25+0.15×usage+0.05。accuracy=1.0、3-5 routes、max_count=50 的技能 → ≈0.32 → F → 30d → deprecate：与稿中自判荒诞的“唯一的罪是不是最常用”（total_routes=1、usage 0.5 → 0.38）同形，只是挪到 ≥3。测试计划却把“≥3 F+30d 仍 deprecate”钉为正例——把同形荒诞钉成正确行为。需加 routing_accuracy 下限（如 deprecate 另要求 accuracy<0.5）或在 §4/CHANGELOG 显式声明“deprecate 仍可因相对低使用率触发，终局裁决按 §6.4 等真实 feedback”。缓解已确认：auto_deprecate 三入口全 opt-in（feedback_loop.py:8-11）、今天燃料为空，故不 BLOCK。支持性发现：模块 docstring 第 4 行“F-grade skills with **sufficient data**”自证现行 `<3` 与自述意图相反，翻转方向正确。
- [MAJOR] 项 4：“只改记录值”对 agent_runtime 侧不成立。`result.skill_id`（agent_runtime.py:561）有非 span 下游消费者：:653 `if result.skill_id:` 门控 skill 注入——今天混合计划 skill_id="fallback-llm" → inject_single_skill 抛错被吞（:664-665），改后真注入该步技能内容，hook additionalContext 载荷变化；:727 instinct feedback bridge 同样消费 result.skill_id。steps[0]→首真步是 hook 输出行为变化，测试计划无此用例。另外 miss 侧取值未定义对称：hook 侧 all-fallback（router_matched=False）仍写 "fallback-llm"、CLI 侧写 ""，两生产者 miss 行 skill_id 不对称，设计应一句话钉死。附带评估结论：发现池组成风险已充分覆盖——bridge `_is_miss` 排除 CLI（tool_call_bridge.py:594）故 outcome 信号不动；gold_detection 明文接纳 CLI miss（gold_detection.py:138-142）；MissCounter/MissedQueryTracker 是 single 模式 UX 机制（cli/main.py:1019）不经 span；MAX_PENDING=50 有 admit-only-if-better 水位（skill_promote.py:101/:1574），~13 行/天量级无风险。
- [NIT] gold_detection.py:125-129 docstring 断言 "the CLI path always did [write the real match verdict]"——恰是 66 行 bug 的反面记载，且 :116-117 “producers set has_match=False … already excludes the fallback_llm sentinel” 对 CLI orchestrated 路径今天为假。项 4 落地必须同步改写该 docstring（gate17 change-one-reread-the-other 惯例），设计未提。
- [NIT] 测量档低于 §1.2 自设规格：只交付热缓存态 online/offline n=3，冷缓存态（未缓存+offline 导出误判路径）无测量，全部推给 e2e。4x 下界结论本身成立（复核 17.7/4.4≈4.0x、Lane C 34.8/5.1≈6.8x，两批方向一致、绝对值漂移有 stderr 限流警告佐证）；spans 中位 16.4s 与复核 online 17.7s 互洽。
- [NIT] 项 5 措辞：evaluate_skill 是公开方法（无下划线），加可选参数是向后兼容的公开签名扩展，非字面“公共签名不动”；所述复杂度 O(distinct×records) 是单调用口径，evaluate_all_skills 总量为 O(distinct²×records)。不影响修复正确性（同值重算论证成立：records 纯函数、单线程流无中途变更；:180 直调路径经内部单遍 fallback 亦降至每调用 O(records)，不加缓存的裁决可信）。
