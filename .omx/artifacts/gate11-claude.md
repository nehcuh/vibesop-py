Review complete — **PASS_WITH_NITS**. The three Track B fixes are verified end-to-end (67/107 reproduced exactly, guard fall-through semantics confirmed at `unified.py:763-794`, no riper/session-end over-triggers remaining), and Track A's hermetic seams faithfully exercise the real CLI code. The one item I'd act on promptly: `scripts/e2e_llm_routing.py` T1/T2/T4 are premised on the now-removed planning scenario and will fail at the next docker e2e run — retargeting it needs a design tweak (no builtin-resolvable scenario remains for a minimal container), not just a fixture swap.
