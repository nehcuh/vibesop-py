先按路由协议匹配技能，再只读核对设计稿里的文件、行号和数据主张。路由落到设计评审，不改文件。接下来对照历史裁决并抽查稿里引用的代码与数据。对照 gate38 §5 后开始抽查代码行号、引用完备性和 cmspark 实测数据。`promote_verifier.py` 路径不对，继续核对 bridge 写侧、引用点和 cmspark 数据。`tool_call_bridge` 不写 `skill_id`，join 必须走 span。接下来用 cmspark 实测文件核对命中率和 `_RouteSpan` 字段。证据已齐：cmspark 行数/join 已实测，引用点已对上行号。按任务书格式给出裁决。## Verdict
PASS_WITH_NITS

## Findings
- [MAJOR] 主项不是换皮分数，但是「2437/2437 全命中」只证明了 `span_id` 键，没证明可归因。cmspark `route_outcomes.jsonl` 3068 行、hit 2437（expired 1268 / reask 1167 / moved_on 2）、miss 631 且无 `side` 键、outcome 行 `skill_id` 全空——这些属实。`span_id → spans.jsonl.id` 2437/2437 命中也属实。但 37/2437 条 hit 对应 span 的 `metadata.skill_id==""`（`has_match=True`、`mode=single`、无 `top_skills`，旧脏 hit）。`skill_health.py:77-78` 已把空 `skill_id` 当非 fire 丢掉。设计把 join 落空定义成「span 不存在」会让这 37 行变成表里的空白 Skill，或与「全命中」测试口径打架。读侧必须与 fire 同谓词：非空 `str` 才入桶，空 id 跳过不计、表中不得出现 `""`；§1.1 改写成 2400/2437 可归因 + 37 跳过。
- [MAJOR] `last_outcome_at` 未钉时间戳。cmspark 2437 条 hit 的 `recorded_at` 全是 `2026-08-23`（回灌日），`span_ts` 从 7 月底铺到 8 月。用 `recorded_at` 则 Last 列恒同一天，「全量计数 + last 已够、不做时间窗」（§1.3）辩护崩掉。钉死 `span_ts`（缺则该行不更新 Last）。
- [MAJOR] 三列分列 + 现有脚注不够防「reask 多=技能差」。moved_on 实测 2/2437，表的辨识度几乎等于 Reask 列；排序未钉，默认按 reask/total 降序就是弱负排行榜。钉 `skill_id` 字典序（或 Last=`span_ts` 倒序并声明非好坏）。§0「这是有没有用的第一个回答」过强——与 gate37 同构的是「已有数据第一次可见」，不是 L2b 效用结论。脚注补实测量级：expired 1268 / reask 1167 / moved_on 2，回灌占 expired 主导。
- [MAJOR] §3 兜底 grep 写成 `RetentionPolicy|retention` 会误伤活符号：`feedback_loop.RetentionSuggestion`、`skill_commands.py:465` `retention_actions`、`config/manager.py:553` `span_retention_days`、`skill_promote.py` retention-pool。收成 `RetentionPolicy|DeprecatedRetentionSuggestion|vibesop.core.skills.retention`。
- [NIT] CLI 形态评审层应锁死：独立 `vibe skill outcomes` 正确（`vibe skills health` 在 `_health.py:75-109` 已被 pack 检查占用）。§1.3 明确不做应加「不把三列加进 `vibe skill list`」——与 `skill_commands.py:161` Fire 30d 并排即视觉拼比率，脚注拦不住。
- [NIT] RetentionPolicy 引用清单不完备。生产 import 只有 `tests/core/skills/test_retention.py:5`（死代码结论成立）；但活文档还有 `GOALS.md:48/:105`、`docs/architecture/skill-runtime-interface.md:265/:341`（仍写 weekly 触发 `analyze_all()`）。§5 文档同步漏这两处。`DeprecatedRetentionSuggestion`（`retention.py:26-48`）与 `RetentionPolicy` 一并点名；「删 :51-190 全类」与「删文件」含混，钉整文件删除。
- [NIT] dashboard 推迟可成立（CLI 已是第一消费方；披露搬运是真漂移面），但重议条件绑 L2b（verdict≥30 ∧ 月 fire≥30）张冠李戴。cmspark `promote_verdicts.jsonl` 不存在（有机 verdict=0）；`/api/skills/health` 原案是 L2-lite 只读面，不依赖 verdict。改为「有真实 dashboard 需求、且愿意把脚注原文打进 payload」再议。
- [NIT] 抽查行号：`promote_verifier.py:11-12` 在 `src/vibesop/core/observability/`（测触发召回，非用后价值）属实；`candidate_manager.py:265` 在 `src/vibesop/core/routing/`。正文应写全路径。`feedback_loop.py:147` 是 `< F_MIN_ROUTES` 那一conjunct，完整 F 规则是 `:143-147`（另需 `days_since>=30`）；薄样本仍是 F-deprecate 唯一燃料（零样本已是 `evaluator.py:94-95` 的 `?`），推迟成立。
- [NIT] bridge 镜像：`tool_call_bridge.py:130/:229/:273` 硬编码 + 显式 `storage_path` 绕过 `span_writer.py:64-69` 属实；pytest 下 `dev_detect.py:54-55` 为真；`test_tool_call_bridge.py:32-33` 写死 `spans.jsonl`，fixture churn 预警属实、代价合理。dashboard 五处硬编码 `:137,:195,:296,:330,:344` 属实，记档 gate40 正确。`span_writer.py:39` 仅 16384 payload 截断、无轮转，容量治理记档正确。
- [NIT] 空 `skill_id` 脏 hit 的写路径（`agent_runtime.py:668` 写 `result.skill_id or ""`）本 gate 零触碰正确，但应记档 gate40，否则读侧会永久吞 1.5% 行。
