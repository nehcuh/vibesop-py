Review complete. All six checks done; measured claims verified where cheap ([executed]: counts, index resolution, calibration precision table; [inspected]: router no-match path, label fidelity vs audit section C, main-set flip analysis).

```
VERDICT: PASS_WITH_NITS
BLOCKS:
- none

NITS:
- tests/benchmark/routing_eval_extended.yaml:1 (header) — 5 of 8 positives (superpowers/using-git-worktrees ×2, superpowers/requesting-code-review, omx/git-master — and the ddo/session-end ids are safe) resolve only when the superpowers + omx packs are installed in the eval env; the header documents the label space but never states the pack requirement. A bare-env run (fresh clone) shows 5 systematic misses that read as router regressions, and a future `build_eval_from_logs.py --merge` would propagate env-dependent positives into routing_eval.yaml. One header line fixes it.
- tests/benchmark/routing_eval_extended.yaml:448 — session-end positive 「现在，我们项目暂时稳定，帮我更新下文档和版本号吧」 carries no exit signal, yet the exit-signal contract (core/skills/session-end/SKILL.md triggers: 离开/走了/收工/再见…) is exactly the standard used to strip 6 other session-end weak labels (L132/L234/L252/L365/L639). Audit called it "可辩护" and the coder followed the audit, but it's the weakest positive and internally inconsistent with the set's own stripping rule.
- tests/benchmark/routing_eval_extended.yaml:74 — L132→omx/git-master is defensible (git-master profile covers commit creation/atomic commits; audit's [] rec was premised on "目录内无 git skill", false once the omx pack is installed), but sibling commit/git queries in the same file (「按批次拆 commit」L436, 「保存并提交远程」L128, 「push到远程」L54) are labeled [] while being equally git-master-shaped. That tension is a designed-in slice of the ~15 env-dependent residuals; fine to keep, worth a note in the header.
- scripts/eval_routing.py:54 — relative --file resolves against Path.cwd() while the default is ROOT-anchored; running from another directory with a relative path silently targets the wrong location (fails only if the file doesn't exist). Anchor both to ROOT.
- scripts/calibrate_index_threshold.py:58-64 — _is_hit still tolerates namespace-less matches ("weak labels often drop the namespace"); labels are canonical post-audit, so the tolerance is stale and could equate same-named skills across namespaces. Harmless with 8 entries; tighten when the set grows.
- tests/benchmark/routing_eval_retention.yaml:13 — retain_until/purge lives only in a comment; nothing reminds anyone on 2026-09-19. Acceptable for a dev repo, but the purge will likely not happen on its own.

NOTES:
- Q1 (no-match semantics): CORRECT. RoutingResult.has_match (src/vibesop/core/models.py:217) is `primary is not None and layer != FALLBACK_LLM`; the fallback path builds exactly that sentinel (skill_id "fallback-llm", layer FALLBACK_LLM, result_mixin.py:298-303), disabled/silent modes return primary=None, and no path sets a real primary with layer NO_MATCH. The router's own miss telemetry (_record_route_miss, unified.py:1201-1207) uses the identical definition, so the harness now asserts the same "no-match" the product acts on. No junk-pass gap: ANY real skill match — even confidence 0.21 junk — makes has_match True and fails the assertion; the assertion is decision-level, stricter than a confidence cutoff. Ternary precedence in the new ok1 parses as documented (expect → reject → no-match). [inspected, plus executed index/calibration runs]
- Q2 (relabeling fidelity): HIGH. Sampled >20 entries across audit buckets — WRONG→[] (L33/45/75/85/111, L464 清理吧 garbage→[]), AMBIGUOUS→[] (L39/91/121/144/210/228/315/335/341/432/542/587), riper over-labels→[] per the skill's ONLY-when-explicit contract (L240/297/328/493/665/809), the four contradictory progress queries now uniformly [], L404/L671 deduped (130 = 106 scored + 23 retained + 1 dropped — math closes). The 8 positives match the claimed composition exactly (ddo ×2 L148/L404, session-end ×2 L271/L448, superpowers ×3 L92/L164/L276, omx/git-master ×1 L74) and all resolve in the live 110-profile index [executed]. 「我先离开了」 hits session-end's trigger list verbatim; the fanout query hits ddo's 深度诊断/审查. Undeclared deviations from audit section C worth knowing: audit-CORRECT scored entries L117/L127/L816 (PI REVIEWER gates, Swift-tray review) and L605 (deep-dive, audit said 改 ddo) were moved to retention under A8's agent-to-agent rule instead of kept; [Image #1] (L852) was moved although the audit said keep-as-negative. All defensible under A8, and the "10 agent-to-agent" move was declared in aggregate.
- Q3 (retention exclusion): GENUINELY EXCLUDED [executed]. Every consumer names files explicitly — eval_routing.py (default + --file), calibrate_index_threshold.py (MAIN+EXTENDED constants), build_eval_from_logs.py (writes extended); tests/benchmark/*.py contain zero yaml references; Makefile `benchmark` runs pytest -m benchmark (collects only .py); CI/pre-commit ignore tests/benchmark entirely. No directory glob anywhere. Purge process is clearly worded; its only weakness is the missing reminder (nit above).
- Q4 (calibration): HONEST, numbers VERIFIED [executed]. Reran the script: main-set bigram precision 0.455 @ 0.05 vs 0.750 @ 0.20 — exactly as cited in manager.py; best-threshold argmax is indeed 0.05 with 12 wrong accepts (vs 2 at 0.20), so rejecting the argmax on precision grounds is data-backed, not narrative. On the audited extended set the index layer has zero accepted coverage ≥ 0.15 and 1/8 unthresholded hits — 8 positives genuinely carry no recalibration signal. "Too thin, keep 0.20" is the right call; the new config comment is accurate.
- Q5 (environment dependence, opinion): real but NOT merge-blocking. The eval is a local, non-CI tool and the baseline narrative already books env-dependent matches as known residuals — but the dependence is undocumented (nit 1), and that's the one thing I'd fix before merge since the whole point of this changeset is honest baselines. Amusing supporting evidence for the residual-error story: the routing hook on THIS review request produced a squad plan whose step 1 skill is "fuck-my-shit-mountain" — over-triggering to locally-installed packs is alive and well, and the cleaned eval set will now measure it instead of laundering it.
- Q6 (main-set regression): SANE [inspected, not executed — embedding-model run skipped]. routing_eval.yaml has 34 entries; only 1 (the deliberate gibberish negative 「xyzq 什么都不相关的乱码查询 blargh」) has empty expect + empty reject and therefore changes meaning — from vacuous pass to a real no-match assertion, which matches the entry's intent. The other 2 empty-expect entries carry reject lists and keep their old branch. 79.4% can only move if the router real-matches pure gibberish, which the layer stack won't. Claimed 55/106 = 51.9% arithmetic checks; oneshot 10/11 and "48 passed" not re-verified (low value vs cost).
```
