先按路由协议匹配评审技能，再读历史裁决与源码证据。路由在跑。同时读技能、历史裁决和被审稿。路由把「设计稿」误配成 UI 评审。按你给的对抗协议做只读技术复审：先读历史裁决，再核源码行号。历史裁决对应章节和源码证据点一起核。开始抽查引用行号，并核 hook 模板、发现池、evaluator 与 F 规则。路由把「设计稿」误配成 UI 评审；按任务书做只读对抗复审。证据行号已抽查，Counter 同值与 F 分公式已在 /tmp 复核。

## Verdict
PASS_WITH_NITS

## Findings
- [MAJOR] 主项 fallback 规格自相矛盾，缓存谓词与注入点未钉。§1.1 写「offline 失败→在线重试」且「不得让 hook 崩」；§1.2 只有「缓存目录存在才导出 HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE，helper 异常不导出」。目录存在 ≠ 快照完整（部分下载/坏 snapshot 仍会导出 offline → `SentenceTransformer` 抛错 → `_layers.py:405-411` / `triage_recall.py:124-127` 现有 fail-open 直接跳过 embedding，**不会**在线重试）。缓存探测也未钉：HF 目录名是 `models--sentence-transformers--paraphrase-multilingual-MiniLM-L12-v2`，代码六处加载有的用裸名（`_layers.py:403`、`triage_recall.py:123`、`indexer.py:480`、`strategies.py:599`、`learner.py:695`），有的带前缀（`observability/embedding.py:50`）。注入点必须覆盖两条热路径、不是「改一处模板」：① `templates/shared/vibesop-route.sh.j2:70-82` 的 `python -c AgentRuntime`（Claude/Kimi/Cursor/OpenCode，**绕过 CLI**）；② `cli/main.py:593-641` `vibe route --hook`（Grok JSON 命令是 `"vibe route --hook"`，`grok_build.py:188`）以及 Pi `vibesop-route.ts.j2:47` 的 `vibe route --json --yes`。Helper 应在 Python 进程内、模型加载前设 `os.environ`（shell 再 spawn 一次 python 探缓存是额外冷启动）。r2 二选一钉死：接受「坏缓存→跳过 embedding」（删掉 §1.1 在线重试），或把逐级 fallback 放到加载点（`local_files_only` 失败再在线）。`_layers.py:403` `triage_recall.py:123` `vibesop-route.sh.j2:70-82` `cli/main.py:593-641` `grok_build.py:188` `vibesop-route.ts.j2:47`
- [MAJOR] 项 2 把 F 闸从「薄样本唯一燃料」翻成「≥3 的 F」，新命中总体未测，且与本稿 §6.4 打架。现规则 `feedback_loop.py:143-148` 确是 `grade==F ∧ days≥30 ∧ total_routes<3`；`total_routes==0` 已是 `"?"`（`evaluator.py:67-72,:92-93`），故燃料只有 n=1/2——与 gate39 §0「薄样本是该规则唯一燃料」一致。翻成 `>=3` 后 deprecate **不是**名存实亡：quality 在无 execution_feedback 时 = `0.25*acc + 0.15*usage_freq + 0.10*health`（sat/success 各 0），acc=1 且 freq<~0.67 → 分 <0.40 → **F**（/tmp 复核：freq=0.5 → 0.375 F；n=3/max=20 → 0.3225 F；只有自己是最常用才到 D）。即：≥3 条路由反馈、无 exec、不是头部常用、闲置 30 天 → deprecate。Lane A 只证旧规则抓「用得少」，**没公布同一语料上新规则命中集**。「今天零行为变化」只覆盖 feedback 为空的现场（B 测 cmspark）；默认 `analyze_all(auto_deprecate=False)`（`cleanup_cmd.py:31`）但 `--auto/--apply` 三处显式入口仍会按新规则写生命周期。archive 加 `>=3`（`:178`）会让 n=1/2 永久不进自动 archive——这是有意滞留，不是零变化。r2：要么本 gate 只改 archive、F 翻闸等真实 feedback（与 §6.4 一致），要么交出旧/新规则命中对照并 CHANGELOG 点名**新人口**（空 exec 污染的 F），不要写成「只对质量差且样本足」。`feedback_loop.py:143-148,:177-178,:60-63` `evaluator.py:54-79,:82-103`
- [MAJOR] 项 4 正文与测试互斥，发现池评估只覆盖 span 一侧。§0.1/§3 写「只改 CLI **span 记录值**、不碰匹配逻辑」；同节测试却写 `OrchestrationResult(plan 全 fallback)→has_match=False`——那是 `models.py:791-795` 的 **property**（`primary 非 fallback ∨ plan.steps 非空`），正是 span 脏 True 的源头（`cli/main.py:914` 写的就是这个 property）。二选一必须钉死：(A) 只改 span 写入 → JSON/`vibe route --json`（Pi `vibesop-route.ts.j2:16,47`）、`eval_routing.py`、确认流仍视 all-fallback 为 hit，发现池与用户面分裂；(B) 改 property → 才是测试所写，blast 是所有 `result.has_match` 消费方，不是「记录值对齐」。发现池：`gold_detection.py:108-143` 明示 CLI miss **合法**进发现；`tool_call_bridge._is_miss:576-598` **排除** CLI，outcomes 不受污染。`miss_counter` 更不受影响——`cli/main.py:1019` 门是 `mode=="single" and not result.has_match`，orchestrated all-fallback 根本不进计数器。66 行/5 天量级可接受，但必须写清「进的是 discovery 不是 miss_counter/outcomes」。hook `steps[0]→首真步`（`agent_runtime.py:560-563`）cmspark 无样本；一行对齐活洞可做，但 `skill_name` 仍取 `steps[0].intent`、`alternatives` 仍从 `steps[1:]`，不一起改会自相矛盾。存量不回写合理（gate39 unjoined）。`cli/main.py:903-914,:1019` `models.py:791-795` `agent_runtime.py:557-563,:668,:692` `gold_detection.py:134-143`
- [NIT] 主项 4x 下界足够立项：两批绝对值漂移（34.8/37.9→5.1 vs 17.7→4.4）方向一致，online 恒慢 13–30s、offline 稳 4–6s。但测量档未交 §1.2 自定的「冷/热缓存两态」；正确性是两则轶事（Lane C tdd 77% vs 复核档 review 82%），不是 n≥3 同 query pin。Grok hook `timeout: 10`（`grok_build.py:189`）对当前 16s+ **已经**会杀进程；修后中位 4.4、样本最大 6.0，余量约 4s。Kimi `timeout = 15`（`kimi_cli.py:181`）、Pi `timeout: 15000`（`vibesop-route.ts.j2:51`）同样贴边。r2 点名超时余量或上调 Grok。`.omx/artifacts/gate40-hook-coldstart.md` `grok_build.py:189` `kimi_cli.py:181`
- [NIT] 项 5 Counter 同值成立（/tmp 空/重复/None/空串全等价）；`get_records()` 是内存列表（`feedback.py:227-230`），analyze_all 热点在 `:201-204` 的 O(distinct×records)，修后亚毫秒投影可信，optimization_service 不加缓存同意。但「公共签名不动 + 把 Counter 传入 evaluate_skill」含糊：无参路径仍走旧双重扫描的话，`optimization_service.py:180`、`_listing.py:244`、`slash_commands.py:367` 各调用仍是 O(distinct×records)。钉死：`evaluate_skill` 默认路径自己 `Counter(...)`，`evaluate_all_skills` 再 hoist 传入。`evaluator.py:167-206,:255-270` `optimization_service.py:180`
- [NIT] 项 1 五行硬编码属实（`server.py:137,:195,:296,:330,:344`），`_spans_filename()` 同谓词、不镜像 exists-gate（`tool_call_bridge.py:140-145` vs `skill_health.py:41-47`）正确；`is_dev_environment()` 实质仅 pytest（`dev_detect.py:42-71`），生产逐字节不变成立。仓内另有 `recall_cmd.py:150` / `trace_cmd.py:437,:522` / `pool_cmd.py:124,:391` / `dag_rebuilder.py:227` 同类硬编码，本 gate 不修需在记档写「非 dashboard、非本项」。`server.py:137,:195,:296,:330,:344`
- [NIT] 项 3 砍成立：`span_retention_days`/`span_max_total`（`manager.py:553-563`）全仓零消费方；轮转先例在 `tool_sequences.py:137-154` 且明确丢未装配条目——自动套到 spans 就是替用户做 gate39 不做的时间窗裁决。重议条件（bridge >2s 或 spans >100MB）可执行。
- [NIT] 换皮：主项不动模型/阈值/`enable_embedding` 默认 False（`manager.py:144-154`）/`_is_agent_prompt_shape`，只动进程环境与加载失败路径，对照 gate34 不做清单 / gate37 §4 通过。项 4 若不改 `OrchestrationResult.has_match` property 则确实不碰三套 trigger；若改 property（见上 MAJOR）仍不是 trigger 语义，但是结果契约变更，不能再用「记录值 bugfix」挡 blast。项 2 是 gate38 §5.4 / gate39 §4.2 明文 deferred 的处置语义裁决，不是新分数。项 5 同值重算。

**主项测量**：4x 下界可信，立项成立。[executed] vibe route 自身 stderr 仍打出 `unauthenticated requests to the HF Hub`，与冷启动叙事同构。[inspected] 引用行号八处均落点正确。
