# gate43 T+14 回声复检 — 测量记录（2026-09-10 补跑）

> 数据源：`~/Projects/cmspark/.vibe/observability/spans.jsonl`（只读，未改动任何数据/代码/配置）。
> 关键时刻：T0 = `2026-08-24T10:42Z`（18:42 CST，gate43 重部署）；T+7 = `2026-08-31T10:42Z`；T+14 = `2026-09-07T10:42Z`（测量窗上界）。
> 口径：完全沿用 `gate43-t7-echo-measure.md` §B.0 锁定值（route span = span_kind=="task" ∧ name 以 `route:` 开头；is_cli = metadata.platform=="vibe-cli" ∨ metadata.source=="cli"（复刻 `tool_call_bridge.py:150/437`）；对 = 同 task_id 的 (hook h, CLI c) 全组合、c.ts > h.ts、锚 = c.ts；回声 = h.has_match==True ∧ Δt≤90s）。
> 校验：本次脚本对 T+7 全部发布数字**逐项精确复现**（基线 611/431、p50 17.9s；T+7 58/25；回声按日 10/5/3/7；pre-14d 253），口径锁定有效。
> 本报告为 gate43-t7 的比对基准并列件（t7 清单要求）。

## 总裁决

| 项 | 裁决 | 一句话 |
|---|---|---|
| 主指标（相对 T+7 不回升） | **FAIL（回升）** | 回声口径 3.57 → **13.00 对/天（+264%）**；宽口径 8.29 → 16.86/天 |
| 绝对 FAIL 线（回到 ≥14.4） | 未触发（13.00 < 14.4） | 但 95% CI [10.6, 16.0]/天**包含** 14.4 |
| 回升归因 | **单日爆发，非趋势性回升** | 91 对中 76 对集中在 9-06（周日）单日；剔除后其余 6 天 15 对 = 2.5/天 ≈ T+7 水平 |
| 护栏三项 | **全绿** | hook 单发 99.7/天（无异常下降）；divergent=0；真人残量 27 > 0（未消失） |

**结论：FAIL（回升），按回滚条款建议见 §5——但附分析意见：回升机制不在文案降级作用面内，建议先 T+21 复检再决定是否触发 revert（本次不执行回滚）。**

---

## 1. 主指标 [executed]

| 口径 | 基线(30.05d) | T+7(7d) | **post (T+7, T+14](7d)** | **full T0..T+14(14d)** |
|---|---|---|---|---|
| **回声（验收）** | 431 = 14.34/天（钉 14.4） | 25 = **3.57/天** | **91 = 13.00/天** | **116 = 8.29/天** |
| 宽口径（对照） | 611 = 20.33/天 | 58 = 8.29/天 | **118 = 16.86/天** | **176 = 12.57/天** |

- **硬验收①（不回升）**：post 13.00/天 vs T+7 3.57/天 → **+264%，回升，FAIL**。full 窗 8.29/天 亦 > 3.57/天。
- **硬验收②（绝对值 ≥14.4 即 FAIL）**：13.00 < 14.4，**未触发**；但 post 91 对的近似 Poisson 95% CI = [74.1, 111.7] 对 = **[10.6, 16.0]/天，区间包含 14.4**，不可判定为"显著低于基线"。
- post 回声按日：09-03=3、09-04=12、**09-06=76**、其余 4 天=0。宽口径按日：09-03=6、09-04=12、09-05=3、09-06=97。
- post 回声 Δt p50 = 19.7s（T+7 28.9s → 变快、更机械）；族内 fast≤30s 占比 88%（T+7 61%、基线 83%）——残余对恢复"机制性"形态。
- 工作日/周末构成：T+7 2/7、post 2/7、full 4/14，均为 **28.6% 对齐 ✓**，无需改 14 天滚动为主口径（滚动基线 pre-14d 253 回声对已复现）。

## 2. 回升归因：9-06 单日爆发 [executed]

post 窗 91 回声对的构成：

- **三通道路由**：h→c 平台交叉 = claude-code→vibe-cli **62**、grok-build→vibe-cli **29**。即每个 prompt 被两套 hook（grok 原生 + claude 兼容通道，即 T+7 §A.5 已识别的双发机制）**再叠加一条 CLI 通道**路由，8–32s 内三次入流。
- **跨 session**：91/91 对 hook 与 CLI **不同 session**（hook 在 01a074xx 会话，CLI 在 6a1ea0d1/51c1a458 等另一格式会话）——不是"同一 agent 收注入后自己重跑 CLI"的形态，CLI 通道来源与 hook 会话分离。
- **query 内容**：9-06 是插件验证 + lane 工作流日（"你现在可以看到我们的插件么？我已签发权限"、"请读取 …/.tmp/lane-status/…"、复审任务 prompt）；h.name 带 `<user_query>` 包裹（hook 原始载荷），c.name 为同一 query 裸文。
- **同源佐证（9-05）**：9-05 单日 169 条 CLI span（mode=orchestrate 165 条，lane 派发任务："开发任务（你是 pi lane）…"），**168/169 无同 task hook 前继** → 当日回声对=0。9-05/9-06 是同一波 lane/插件工作流的两天，CLI 通道量级暴涨（见 §3）。
- **结论**：回升不是文案降级效果消退（稳态日 0–12 对/天，剔除 9-06 后 2.5/天 ≈ T+7 水平），而是**单日工作流爆发**（lane 派发 + 插件验证）撞上 T+7 §A.5 双发机制的三通道变体。

## 3. 护栏复核 [executed]

| 护栏 | 基线30d | T+7 | post | 判定 |
|---|---|---|---|---|
| hook route span 量 | 118.3/天 | 109.6/天 | 113.1/天 | 无故障 ✓ |
| **hook 单发量**（无同 task CLI 后继 ≤90s） | 100.0/天 | 102.3/天 | **99.7/天** | **无异常下降 ✓** |
| CLI route span 量 | 16.7/天 | 5.6/天 | **35.1/天** | 使用模式变化（§2） |
| divergent 伪 miss（前继 hook 命中 ∧ Δt≤300s） | 28* | 0 | **0** | **= 0 ✓** |
| 真人 CLI miss 残量（slow + cli_only） | 10 | 3 | **27** | **未同步消失 ✓**（升） |

- CLI miss 分解（has_match==False 的 CLI span）：universe 总量复现 t7 发布值（基线 60 / T+7 11）✓；post = 30（cli_only 27、hook_miss 前继 3、slow 0、divergent 0）。残量上升全部来自 9-05 lane 派发的 CLI 调用，属使用模式而非文案退化。
- 注：t7 发布表中 fast_echo/divergent 的精确切分（22/28）在简单阈值变体下无法逐格复现（27/23 等），疑似 lane 内部 Δt/hit 源细节；但 universe 总量与 divergent=0 结论对任意变体稳健 [inspected 限制声明]。

## 4. 混杂因素陈述（task 要求）

1. **agent 使用模式变化（主导）**：9-05/9-06 lane 工作流 + 插件验证爆发——9-05 单日 169 条 CLI 派发、9-06 单日 285 条 route span（含 111 span 的单 session）；CLI 通道周量 ×6.3（5.6→35.1/天）。非文案失效。
2. **A.5 双发机制叠加**：91/91 回声对都含 grok-build + claude-code 双 hook 前继；该机制 T+7 已定性为与 gate43 正交（hook 层面重复注册），本次是它首次与 CLI 通道同帧出现。
3. **工作日构成**：对齐（28.6% = 28.6%），**不构成混杂**。
4. 单日爆发：76/91 在 9-06 一天，窗内分布极端非平稳；7 天窗口均值对单日事件敏感。

## 5. 收尾建议

- **正式裁决**：FAIL（相对 T+7 回升 +264%）。按 synthesis 回滚条款的形式建议 = **revert + 重 build**（未执行、也不自行执行）。
- **分析意见（供仲裁）**：回升机制（跨 session 的 harness/插件 CLI 通道 × 双 hook）**不在文案降级的作用面内**——文案只影响"同一 agent 收到注入后是否重跑 CLI"，而 91/91 对的 CLI 与 hook 分属不同 session；revert 无法消除该机制，且会永久恢复 14.4/天量级的文案驱动 echo。**建议先做 T+21 复检**（或剔除 9-06 单日后的窗内复检），确认稳态（2.5/天 ≈ T+7）无趋势性回升后再定回滚去留。
- 遗留（t7 清单项）：② gate42 A.1–A.3 幻影绝对计数复检未在本轮执行（本次任务 operative spec 未含，如需可补跑）；③ A.5 双发机制未见立项痕迹（grep docs/ROADMAP 无专门 gate），本报告 §2 交叉表（post dt≤300s：claude-code→vibe-cli 78、grok-build→vibe-cli 37）可作趋势基线。

---

## 证据分级汇总

- [executed]：全部数字为对 `spans.jsonl` 的直接只读计算（一次性脚本，未落仓）；T+7 全部发布值逐项精确复现作为口径有效性证据。
- [inspected]：is_cli/route span 谓词复刻自 `tool_call_bridge.py:150/406-441` 与 `_span_fields.py:14`；§3 分解切分限制声明。
- [assumed]：9-06 CLI 通道来源（harness/插件）未做逐 span 溯源到调用进程，依据为跨 session 结构 + query 内容 + 使用日特征推断；CI 为近似 Poisson（T+7 用精确分位数，差 <0.5 对，不影响判定）。
