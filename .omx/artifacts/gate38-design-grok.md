先按路由协议匹配技能，再只读核查 gate38 综合稿与历史裁决。路由结果偏 meta，任务本身是只读设计复审。接着读历史裁决与被审稿，并抽查代码引用。对照历史裁决后开始抽查设计稿里的文件行号与调用点。部分行号已对不上。接着核对 CI 工作流、CLI span 写入，以及 feedback_loop 的精确语义。抽查完引用与调用点。`evaluate_all_skills` 的纳入口径和 `_health.py` 的 `"?"` 展示还要再对一下。## Verdict
PASS_WITH_NITS

## Findings
- [MAJOR] 假 L2 核心伤害链写错，联合 must-NOT 会测空。生产 `evaluate_all_skills` 只纳入 routing feedback / preferences / execution feedback，**不扫** `record_usage` 写入的 `usage_stats`（`evaluator.py:246-254` vs `candidate_manager.py:261-276`）。即便进了循环，零样本默认分 0.5→D（`evaluator.py:64-66,85-93`），规则顺序是 D+60d **先 return warn**（`feedback_loop.py:139-152`），永远走不到 `:155` archive；warn 即使 `auto_deprecate=True` 也不写生命周期。现状零样本+90d+auto=True **本来就不 `set_lifecycle`**。联合测试若只 `assert_not_called`，改前就会绿。必须用真实 `SkillEvaluation(total_routes=0, last_used=90d前)` 断言 `grade=="?"` 且 **suggestions 为空**（含无 warn）。
- [MAJOR] `quality_score=0.0` 与 `grade="?"` 必须同一 commit，且压过 §6「多分 commit」。若只改分数不短路 `grade()`：0.0→F，F+30d+`<3` 走 deprecate（`feedback_loop.py:119-125`），比现状 D/warn **更糟**。§2.1「单提交原子」与 §6「单 gate 多分 commit」互相矛盾——evaluator 两返回值禁止拆开。
- [MAJOR] §2.2.8「`stale --auto` 是唯一自动入口」与 §2.3 复活 `optimize --apply`（`analyze_all(auto_deprecate=True)`）直接矛盾。`--apply` 无 `--yes` 确认（`optimize_cmd.py:31-38`），help 写「Apply safe optimizations automatically」。落地后 help 文案会说谎。二选一：删 `--apply`（B 案）并把 uniqueness 写真；或承认两入口、改 stale help、CHANGELOG 并列。另：`analyze_all(True)` 会顺带 `_apply_boost` 把 deprecated A 拉回 active（`feedback_loop.py:89-90,196-202`），log 却只收 `{deprecate,archive}`（§2.3）——复活范围大于文案。
- [MAJOR] `_load_quality_actions` 修好 `FeedbackLoop(evaluator)` 构造 bug（实在 `:105` 不是 `:106`）后，若漏 `auto_deprecate=False`，**无 `--apply` 的 `vibe optimize` 展示报告就会写生命周期**。测试计划只有 `--apply` 正例/无候选反例，缺 dry-run must-NOT。
- [MAJOR] `"?"` 展示兼容性核查不成立。`_health.py:156-162` 非 A/B/C/D 一律 🗑️；零样本再加 `quality_score:.0%` → `? (0%)` + 垃圾桶（`_quality.py:102`、`slash_commands.py:372-384` 同）。`_listing.py:96` 有 `total_routes>=3` 才免疫。`status_cmd.py:70-44` 的 grade bar 只列 A–F，`"?"` 进 `evaluated` 分母却不上柱。§2.1「全部 dict.get 落 dim/—」不真。
- [MAJOR] hit/fire 双总体诊断对，缓解不够。gate37 修订 B 补丁 fire **含 CLI**（`skill_health.py:55-57`）；hit outcome 排除 CLI。docstring 禁止拼比率，但行 schema 只有 `side:"hit"`，不自描述 population；§5.1 已把 dashboard health 列为下一刀。至少：outcome 行加 `"population":"hook"`（或等价），L2-lite/dashboard 加 must-NOT 禁止用 hit outcome 当成功率。更优于纯披露：CLI hit 也落盘但 `eligible:false`，避免再丢掉一类写时数据。
- [NIT] 换皮总判成立：三项确是 gate37 裁决 2/4 + 修订 C deferred；无 intake/阈值/hash chain；CI 双保险（`eval_routing.py:174` 恒 0 + `continue-on-error`）不是硬阻断。但「本稿删除自动处置」与复活 `--apply` 并置，叙事上像换皮，须在 §0.1 改写。`--strict` 留口（§3.2）是硬阻断枪，即使 CI 不接；更干净是本 gate 不造这个 API。
- [NIT] 不变量：设计触达文件不含 `skill_promote.py:366`、upsert、embedding、`_is_miss`/`_classify` 函数体；hit 为新函数。零触碰成立，前提是实施不顺手改 miss 测试。
- [NIT] 裁决 1.0 top_skills「写时不可回填」方向对（现 span 只写 winner `skill_id`，`agent_runtime.py:668`）。无消费方不违反最小化——最小化禁的是假指标不是 3 个 string。但「不得派生比率」只写模块 docstring，与 hit 披露同样软；CLI miss 的 `result.alternatives` 是 fallback nearest（`result_mixin.py:277-328`），hook miss 在 `if primary` 内才填 alternatives（`agent_runtime.py:619`）——两侧 top_skills 语义不对齐，schema 应写明仅 `has_match is True` 才写键。
- [NIT] 裁决 1.2 docstring 披露不够当唯一闸。命中弱阳性首跑会把几乎全部历史 hook hit 写成 `hit_session_expired`（`SESSION_COMPLETE_HOURS=24`，`tool_call_bridge.py:90,537`），与「为 L2b 攒数据」目标相悖。可只回填有 reask/moved_on 证据的 hit，expiry 仅对 gate38 之后增量生效。
- [NIT] 裁决 2.4：在 uniqueness 改写后「修而非删」可成立（显式 flag ≠ 静默 auto）。B 的「无人用」被构造 bug 污染，不能当需求证据。但修的同时必须收紧范围（dry-run 不写、boost/restore 要么 log 要么不做）。
- [NIT] 行号：`optimize_cmd.py:106` 构造 bug 实际 `:105`（`:106` 是 `analyze_all()`）；`release.yml:15-21` 是 `uses:` 调用方，`workflow_call` 在 `ci.yml:11`；`dashboard/server.py:137` 是 `spans_path`，metadata 容忍解析在 `:73-82,:128-133`；`aggregator.py:146` 是 `token_accounting` 特判不是通用 metadata walk。其余抽查属实：`evaluator.py:64-66`、`feedback_loop.py:66/196-204/208/246`、`optimize_cmd.py:140-157`（`apply_auto_actions` 不存在）、`tool_call_bridge.py:414-539,:97,:195,:470-493`、`agent_runtime.py:620-626/667-700`、`cli/main.py:899-936/1707`、`eval_routing.py:80,174`、`ci.yml:110`、`render.py:66/:248`、`skill_commands.py:355-373`（`--json` 随后 `:378 generate_report()` 默认 True，静默处置属实）、`optimization_service.py:184`、`matcher_pipeline.py:133`、`external_loader.py:124`、extended yaml `:100-104/:118-121/:190-193/:302-304`、`test_evaluation.py:40-50`、`test_tool_call_bridge.py:496`。
- [NIT] 测试缺口：`vibe optimize` 无 `--apply` 不写生命周期；`stale --json` 不写；`quality_score=0.0` 不得让 `grade()=="F"`；eval `ok1 is None` 时 `hits1 += ok1` 会 TypeError，skipped 全员时 `hits1/total` 除零（`eval_routing.py:91,128`）；`requires_packs` + `expect:[]` 时 `all([]) is True` 会空跳（当前 4 条 expect 非空，规则泛化时踩）。`test-windows` 的 `continue-on-error` 注释写明观察期满要升硬门（`ci.yml:105-110`），routing-eval 必须写明**永久** report-only，避免被一次性清掉。
- [NIT] 文档清单漏 `GOALS.md:104` / `ARCHITECTURE.md:597`（仍宣传 FeedbackLoop 自动降级）。`retention.py:6-10,72` 已有 grade `"?"` = 无评价对象，evaluator 新 `"?"` = 有对象无路由反馈，两词表同符不同义，docstring 要分开。
