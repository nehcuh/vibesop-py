先按路由协议匹配技能，再只读核对 r2 对第一轮 MAJOR 的处置与重设计边界。路由命令超时了，先并行拉评审稿、第一轮 grok finding，以及相关源码核对点。路由命中 `kimi-gated-fix`，但本任务是只读设计确认复审、输出格式已钉死，按用户指令走确认制核查。接下来读第一轮 grok 评审与关键源码。第一轮 grok 三条 MAJOR 已对照 r2 处置表。接下来核加载点、fail-open、项 4 写值/读侧谓词，以及 fire 列 sentinel 的实际影响。加载点 fail-open 形态已经分叉。接着核 CLI `top_skills` 闸、hook 写点、`result.skill_id` 消费者，以及 fire 列里 `fallback-llm` 的实际数字。CLI orchestrated 的 `primary` 为空，`skill_id` 全空——这会改变 fire 列口径。接着量化两仓 spans，并核 helper 异常边界。## Verdict
PASS_WITH_NITS

## Findings
- [MAJOR] 主项 helper 的 fail-open 边界未钉，和「双败后行为与今天完全相同」矛盾。六处加载点 fail-open 形态本就不一：`_layers.py:405-411` / `triage_recall.py:124-127` / `indexer.py:481-483` / `promote_verifier.py:137-140` 是 `except Exception`；`learner.py:697` 只吃 `(ImportError, OSError, RuntimeError)`；`strategies.py:600-604` **只吃 ImportError**（加载失败今天就会冒泡）。§1.2 写成「异常→在线重试，双败→既有 fail-open」。若 helper 用裸 `except Exception`：① ImportError 会做一次无意义在线构造；② 坏缓存/OOM/非缺失类失败会从今天的毫秒级 fail-open 变成 13–30s 在线重试，Grok hook `grok_build.py:189` `timeout: 10` 会在 fail-open 代码跑到之前杀进程。冷缓存首次下载撞 timeout 已披露，但「行为与今天完全相同」把这条杀进程路径洗掉了。r2 必须给出伪码：`ImportError`/`KeyboardInterrupt`/`SystemExit`/`MemoryError` 不重试；仅缓存缺失类再 `SentenceTransformer(name)`；二次异常原样抛给各点既有 except。测试加「非缺失类单败 → 零在线调用」。`local_files_only` 本身成立（[executed] st 5.7.0 有该参数）。
- [MAJOR] 读侧 `"fallback-llm"` 排除对 fire/outcomes 既有数字的披露按本仓 6 行写成「噪音行消失」，cmspark 生产量级未测。`skill_health.py:50-78` 与 `skill_outcomes.py:92` 共享 `_route_hit_skill_id`：cmspark 30d fire 1061/2822（37.6%，当前最大桶；全时 1088），hit-outcome 1088/2440（44.6%）会整桶从技能列进顶层 `fallback`；1086/1088 是 M12 前化石，2 条 08-23 是活洞 B。本仓才是 6 行。另一向未披露：CLI orchestrated **一律** `primary is None`（`orchestrator.py:472-478`），`cli/main.py:903-906` 写 `skill_id=""`，不只 all-fallback——cmspark 69 / 本仓 11 条 CLI orchestrated hit 今天都不进 fire；§3.2「skill_id 取首个真步」会让其中有真步的开始进 fire。§7 实施时补口径填不满这篇已经写死的「噪音」叙事。CHANGELOG/测量档必须同时给出 cmspark 掉量（fire 1061、outcomes 1088）和 CLI orchestrated 潜在增量，且不得把 44% outcome 行叫噪音而不写数。
- [NIT] 项 4 测试计划的 property 不变 pin 与 span 写值翻转，没覆盖 `top_skills` 共存。hook `agent_runtime.py:668` 将改 span `skill_id`，但 `:714-724` 仍用不动的 `result.skill_id`（steps[0]）组 `top_skills`，会打破 `:708-711`「与 skill_id 刻意重复」不变量：首步 fallback+次步真 → span `skill_id=次步`、`top_skills[0]=fallback-llm`。CLI `:924` 仍闸在 **property** 而非新 span 谓词；生产 all-fallback `primary is None` 所以 `top_skills` 本来就不写，但 `tests/cli/test_route_cli_task_id.py:247-251` 文档写死「与 metadata.has_match 同一表达式」。测试要钉死二选一：top_skills[0] 仍跟 result.skill_id（契约不动），或写点改成与 span skill_id 同谓词。
- [NIT] `_route_hit_skill_id` 对 `"fallback-llm"` 返回 `None` 会与空 skill_id 塌缩。`skill_outcomes.py:148-155` 今天把 map 未命中打进 `unjoined`；若不在 helper 之外再读 span.skill_id，cmspark 那 1088 行会进 unjoined 而不是新的 `fallback`，对账式 `Σ三列+unjoined+fallback=hit` 表面能过、语义错。测试钉死：存量 fallback-llm → `fallback` 计数、不进 unjoined、不进 per-skill 三列。
