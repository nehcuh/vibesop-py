# Lane E — 产物持久化收口

你是 **pi**，在 worktree `/Users/huchen/Projects/vibesop-evo-E-pi`、分支 `feat/evo-E` 上独立施工。

先读：

1. `docs/specs/2026-09-11-evolution-optimization-plan.md`（总计划；若本 worktree 没有，读 `/Users/huchen/Projects/vibesop-py/docs/specs/2026-09-11-evolution-optimization-plan.md`）
2. `/Users/huchen/Projects/vibesop-py/docs/specs/2026-09-11-evolution-direction-proposal.md` 方向 E
3. `/Users/huchen/Projects/vibesop-py/.gitignore`（注意第 143 行 `.claude/`）
4. `/Users/huchen/Projects/vibesop-py/docs/ROADMAP.md`（找对 `.omx/artifacts/` 的引用）

先写 8 行以内的实施计划到 `.omx/artifacts/evo-lane-E-plan.md`，然后直接动手。不要等确认。

## 只许改

- `.gitignore`
- `scripts/check_artifact_links.py`（新建）
- `tests/unit/test_check_artifact_links.py`（新建）
- 把仓库内 `.claude/commands/` 与 `.claude/hooks/` 变成可跟踪（不要加入 `settings.local.json` / `worktrees/`）

## 不许改

- `.git/info/exclude`（grok 在你的 gitignore 合入后再删 `.omx/` 行）
- `src/` 产品代码
- R3/R4 prereg、`behavior_consistency.py`、`routing/benchmark.py`
- 其他 lane 的文件

## 要做的三件事

### E1 `.gitignore` 显式边界

删除「整目录忽略 `.claude/`」这种一刀切（若存在）。改为：

```
# OMX ephemeral — artifacts/ 默认跟踪，不要忽略
.omx/tmp/
.omx/scratch/
.omx/cache/
.omx/runtime/
.omx/logs/
.omx/state/

# Claude Code local-only
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.json
```

原则：被 tracked 文档引用或作为验收证据的路径必须可 `git add` 而不需要 `-f`。

不要在 gitignore 里写 `.omx/` 或 `.omx/artifacts/`。

### E2 断链守卫

`scripts/check_artifact_links.py`：

- 扫描 tracked / 给定根下的 markdown（至少 `docs/`、`README.md`、`CHANGELOG.md`、`ROADMAP.md`）
- 找出指向 `.omx/artifacts/...` 的链接或反引号路径
- 用 `git ls-files`（可注入）判断目标是否被跟踪
- 缺跟踪 → exit 1；全绿 → exit 0
- `--root` 可测

单测（tmp git repo fixture）：

1. 文档引用一个 tracked artifact → exit 0
2. 文档引用一个未跟踪文件 → exit 1
3. 文档无 artifact 引用 → exit 0

### E3 `.claude/` 拆分

在本 worktree 把 `.claude/commands/` 和 `.claude/hooks/` 加入索引（从主仓库 `/Users/huchen/Projects/vibesop-py/.claude/` 复制过来，如果 worktree 因 gitignore 没有这些文件）。不要提交 `settings.local.json`。

## 提交

```
git add <具体文件>
git commit -m "feat(obs): track omx artifacts and guard dangling links"
```

不要 push。不要 `git add -A`。

## Handback

写 `.omx/artifacts/evo-lane-E-handback.md`，必须含命令和退出码：

```
uv run pytest tests/unit/test_check_artifact_links.py -q
uv run ruff check scripts/check_artifact_links.py tests/unit/test_check_artifact_links.py
git status --short
```

Status 只能是 DONE / PARTIAL / BLOCKED。编造「已完成」视为失败。
