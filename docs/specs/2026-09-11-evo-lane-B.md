# Lane B — CI decision_source 注册表（Wave 2 草案，不可终裁）

先读：

1. 提案方向 B
2. `.github/workflows/ci.yml` 全部 job
3. 总计划：零新门禁里「LLM 评审当放行闸」这一条要变成机制

## 只许改

- `ci/decision-source.yaml`（新建）
- `scripts/check_ci_decision_source.py`（新建）
- `tests/unit/test_check_ci_decision_source.py`（新建，用 fixture workflow，不要改真 ci.yml）

## 不许改

- `.github/workflows/ci.yml` 本身（本轮只交叉核对，不改 CI）
- 产品代码

## 注册表草案（grok 预填，**待人确认**）

从 `ci.yml` 抽出的 required jobs（2026-09-11）：

| job id | name | 建议 decision_source | 理由 |
|---|---|---|---|
| lint | Lint | deterministic | ruff |
| type-check | Type Check | deterministic | basedpyright |
| test | Test (Python …) | deterministic | pytest |
| test-windows | Test Windows | deterministic | pytest |
| security | security | deterministic | bandit / pip-audit |
| benchmark | benchmark | deterministic | 现有 hermetic 基准 |
| routing-eval | routing-eval | deterministic | eval_routing hermetic |
| routing-benchmark | routing-benchmark | deterministic | 同左 |

脚本行为：

- 真 `ci.yml` 的每个 job 必须出现在注册表；缺登记 → exit 1
- 注册表多出来的 job → exit 1（防漂移）
- `decision_source` 只能是 `deterministic | human`
- 若值为其它（如 `model`）且该 job 在 workflow 里不是 `if: false` 的 optional → exit 1
- B2 先不做 import 扫描（等 B1 草案被接受）

单测：fixture workflow 注入一个带 `decision_source: model` 的 required job → 红灯；覆盖 100% job → 绿灯。

Handback 必须列出「需要人点头的 job 清单」。不要把草案写成已经终裁。
