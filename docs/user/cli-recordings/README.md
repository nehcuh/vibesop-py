# CLI recordings

Transcripts captured in Docker for [COMMAND_HANDBOOK.md](../COMMAND_HANDBOOK.md).

```bash
# rebuild recordings
bash scripts/record_cli_handbook.sh          # docker: vibesop-val-base:py3.12
HOST=1 bash scripts/record_cli_handbook.sh   # host uv
```

Image: `vibesop-val-base:py3.12` · cwd `/repo` · `HF_HUB_OFFLINE=1` · `NO_COLOR=1`
Date of current files: 2026-09-07
