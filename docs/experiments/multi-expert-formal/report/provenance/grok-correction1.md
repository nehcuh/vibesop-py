监督者纠偏，请在当前会话继续实施，不重启任务、不重跑 route。我已中断上一轮避免随后校准出现记录污染。继续读主目录 .experiment/beliefs.md 中新增的 supervisor notes，必须在正式冻结前落实：VWAP命名、跨层题复杂度/范围、完整规范可查询、T4顺序一致。

刚审查到必须立即修复的客观问题（尚未正式运行）：
1 harness/engine.py Loop.__init__把self.calls=0。C/D/E创建多个Loop指向同一个run_dir，各自从call-00写起，会覆盖先前成员/阶段请求响应。改为全程唯一、单调、持久call_id（ledger/run recorder分配），所有attempt原子创建禁止覆盖；离线测试要覆盖C/D/E多个Loop，并核验raw response用量之和等于ledger。工具事件也要全程保留，notes不能只保留最终整合者。
2 请求日志现在complete()成功返回后才写。必须调用前落盘完整请求（包括工具schema或其冻结hash）、调用开始时间；API出错仍有请求+错误。不能先调用后补请求。
3 B plan提示“Do not call deliver until asked”，但Loop只在deliver退出。改为明确的“以阶段完成报告结束规划，之后进入实现/检查”，保持完整历史；规划阶段不能死循环到预算耗尽。
4 max_turns=16是每Loop而不是全程，A最多16但B/C/D/E多个Loop可能更多。不得暗中增加组织不同的调用上限；统一全程模型调用上限或依赖同一T/I/K+防挂超时，记录实际限制。
5 C/D的交流目前三人共享一个stage余额，首人可能用完，后两人无法交流；E也类似。保证每个已分配成员可以完成规定阶段，明确子额度/未用额度转移。内部阶段耗尽不要误判全程预算耗尽；长度截断可能包含未完成tool_call，不能把无匹配tool结果的消息直接送回API。用离线长响应/截断测试验证。
6 E计划必须校验唯一worker id、已知依赖、无循环、文件边界。当前依据“是否有deps”排顺序不能验证DAG；有重叠文件须按预注册认定违规/明确冲突处理，不可只记一条却依然标成功。整合者应保留规划阶段历史，不能重新起一个只拿截断6000字符worker trace的context而宣称保持完整历史。

以上是监督检查，不需要用户批准。你负责具体修改与验证。继续既有第一阶段任务，准备未达标就修，准备达标写READY_FOR_REVIEW后停给我复核；不要启动360。每个实质里程碑更新STATUS。还没有commit/eval的首轮请按规定先commit后eval，再更新实际belief/结果账本。保持历史校准不变。不要只是回复承认问题，实际修到测试通过。
