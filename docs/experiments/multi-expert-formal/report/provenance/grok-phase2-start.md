监督者已审查第一阶段材料，并抽查全部15条K80原始响应用量：均与ledger一致。用户仍要求你执行完整实验，我继续负责方向与重要结果，不让用户重复批准。

方向决定：接受12任务的受控小型CLI基准范围、五组定义，以及统一 T=24000/I=400000/K=120/hang=600。K40失败是该资源条件下的真实成本观察，与日志覆盖/阶段退出缺陷分开，不称一切预算失败为假象。不得将同一模型多实例+角色提示推广为异质真实专家团队或整个代码仓库的结论。

当前READY有几个必须补齐的运行/统计门槛，主目录 .experiment/beliefs.md 已说明，但尚未在代码落实。你具体实施以下修改并验证。不要重做已通过且源哈希未变的Docker参考/错误变体全套；保留并校验既有通过证据，运行与这次改动有关的离线/集成检查。仍先预测、commit、eval、记结果。优化循环只针对harness，不淘汰样本。

1. 正式可恢复执行：完整360分配一次性写入不可变manifest，始终保留360（不是先切成12再写manifest）。增加 --max-new 12 只执行本次前12个尚未分配执行的任务；--resume <run_root> 继续同一个manifest剩余任务。已完成passed/failed/error不重跑不覆盖。中断的running要明确记为interrupted并保存原调用，不无声补跑；不清理已有产物。防重复运行同一allocation，恢复时核验源/任务/资源哈希。
2. 固定并发8个独立运行（Docker环境实测18CPU、约16GB内存，可容纳8×256MB），每个运行独立client/ledger/workspace/recorder；不要并发共享同一个运行上下文。把并发数、随机分配seed和执行方式入冻结清单。先离线/非正式隔离检查证明并发下不会碰目录、覆盖日志、错记usage。这里不比较墙钟速度。
3. 完整冻结包：保存所有实际执行的harness代码、任务/spec/QA、hidden evaluator/oracles/cases、工具schema、资源、统计预注册与分析代码，保留相对路径和文件内容+sha256。排除不断增长的logs/runs/STATUS/READY/verify-latest等运行产物。之前仅5个basename副本且rglob将日志当源码的做法不能作为正式freeze。启动/恢复校验冻结内容；运行时使用被冻结的代码或保证live源码与freeze逐项相同。配置缺失/非法要error，不允许fallback默认资源。未知文件不应被无意装入参与者workspace。固定Docker实际image id（而不是只记录可变tag）。
4. 把统计方法与完整阈值规则先写入preregistration并提交：主比较D-A，每task含full/brief×3repeat共6个配对观测，计算task内平均差，再对12个task等权平均。95% cluster bootstrap percentile interval，20000次，seed=20260913；一次抽12个task索引有放回，每个被抽中的task保留所有spec与repeat；失败/error/interrupted计0。lower>+10pp才支持预定采用阈值；upper<+10pp只排除这一最低收益，非证明等价；upper<0支持更差；区间跨阈值未定；全区间在0与+10之间是小正效应但不足采用阈值。其余比较探索，不做未注册显著性宣传。先实现分析脚本并用已知toy数据校验符号/聚类/端点，正式结果出来后不可换方法。
5. 完整报告应输出REPORT.md、逐次机器可读结果、每任务/规格/组表、主差值和区间、实际T/I/K及缓存/推理成本、失败分类、3-5条可回查轨迹与代码/验收证据、限制与建议。图用标准绘图工具生成通过率/成本及任务矩阵等少量有用图，不能只给总体胜率。已失败但没有hidden evaluation时不称代码有bug。必要的少量事后诊断可单列，绝不改变主指标/重新向模型反馈隐藏答案。

执行授权：以上门槛检查通过、提交并冻结后，你可以直接启动同一个360正式manifest的前12个分配（--max-new 12），不需再向用户或监督者求许可。这里是日志/冻结/恢复的早期审计点，不按成绩决定是否继续。完成12个后写 FIRST12_AUDIT.md（全部记录对应、ledger对账、无隐藏泄漏、冻结未漂移、下一条resume命令）并结束本轮headless给我检查；不要重抽任务，不以胜负为停止理由。我审计后让你恢复剩余348并完成报告。若12个包含实验工具/协议错误，留存原样，明确错误，不自动删除/重开正式cohort。

先读本文件和beliefs，落实即可，不要只回复计划。所有实现留在实验worktree docs/experiments/**；主工作区只写 .experiment 元数据。历史75条校准（45旧+30新）不修改。任务合同已接受，不再随意加题/改题；上述仅修正式运行冻结/计量/统计流程。准备和formal各阶段持续更新STATUS.md。

报告正文请用中文，面向关心实际AI开发方法的读者；代码、字段名和原始日志保留原语言。不要把准备期的未完成检查包装为正式结论。
