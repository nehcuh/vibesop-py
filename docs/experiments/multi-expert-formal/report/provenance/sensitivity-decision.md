# Supervisor prospective sensitivity decision

Recorded at 2026-09-13T02:43:40.012106+00:00 after first12 integrity audit and 17 completed primary allocations, before remaining primary outcomes.

Trigger observation: 5/5 first12 committee C/D allocations fail at independent-0 cap, before integration/hidden evaluation. This is a hard stage-termination mechanism, not evidence of worse code. Primary360 must continue unchanged and be fully retained.

After primary360, conduct a separate explicitly exploratory sensitivity cohort if internal stage-cap termination remains a substantial contributor (at least half of D failures). Do not pool or replace primary data.

Design: same12 tasks × same2 specs × A(single unchanged) / D_soft(expert committee with soft internal-stage completion) ×3 repeats =144. New immutable randomized manifest, seed20260914; same served model/request params/tools/T24000/I400000/K120/hang600/concurrency8/hidden acceptance/tasks. Same task-paired bootstrap20k seed20260913,12 clusters, same10pp threshold, explicitly exploratory adaptation motivated by primary diagnostics.

Single mechanism change: in D_soft, catch only stage_budget_exhausted for independent-* and exchange-*; preserve full observed histories and on-disk private files, record soft-close event, then continue the pre-existing arm sequence and unchanged unused-token transfers. Do not increase any stage or global quota; do not alter analysis/role/prompt/plan/task. Existing missing deliver report remains absent (may mark missing transparently in metadata, not synthesize expert opinions or consume extra model calls). Final integrate, global resource/API errors, invalid output/ownership all follow original hard failure rules. A uses the same newly running cohort and original behavior. Do not compare only newDsoft to historicalA.

Keep immutable primary sources and copied frozen analysis. Implement sensitivity as separate modules/copy outside primary freeze; save exact diff and its pre-call hashes. Tests must force a partial independent stage, show next members and integration reached, no stage/global budget overruns, hard integrate/API failure still fails; prove A unchanged. Gold/hidden tasks identical hash. All144 including failures stay denominator.

This tests sensitivity to one orchestration termination rule, not best possible multi-expert capability. It provides an end-to-end comparison that can distinguish a brittle pipeline from a general teamwork claim.
