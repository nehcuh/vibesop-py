# VibeSOP 哲学

> **版本**: 5.4.1
> **核心使命**: 成为 AI 辅助开发的技能操作系统（SkillOS）
> **更新时间**: 2026-05-01

---

## 我们的信仰

### 核心信念

**1. 发现 > 执行**

AI 工具已经强大到可以执行大多数任务。真正的问题是：**找到正确的工具**。

- ❌ **错误**: "让 AI 更强大" → 已经够强大了
- ✅ **正确**: "让找工具更容易" → 这是真正的瓶颈

### 2. 匹配 > 猜测

人类的直觉和记忆是有限的：
- 你记不住 45+ 个技能的命令
- 你无法准确判断每个场景该用什么
- 猜测会导致错误的工具选择

- ❌ **错误**: "记住更多命令" → 违反人性
- ✅ **正确**: "智能匹配意图" → 符合认知规律

### 3. 记忆 > 智能

"智能"往往是幻觉。真正的智慧是**记住什么有效**。

- ❌ **错误**: "让 AI 更智能" → AI 已经很聪明了
- ✅ **正确**: "记住用户选择" → 数据驱动的优化

### 4. 开放 > 封闭

没有单一工具可以解决所有问题。真正强大的是**生态**。

- ❌ **错误**: "绑定一个平台" → 被平台锁定
- ✅ **正确**: "跨平台通用" → 工具可替换

---

## 我们的使命

### 愿景

AI 辅助开发的工具爆炸式增长：
- Claude Code, Cursor, Continue.dev, Aider, Cline...
- 每个工具都有自己的命令和技能
- superpowers, gstack, omx 等技能包蓬勃发展
- 用户不知道该用哪个

### 问题

**认知负荷**: 用户需要记忆 45+ 个技能和命令

**决策困难**: 不知道哪个技能最适合当前场景

**学习曲线**: 每个新工具都要重新学习

**碎片化体验**: 不同工具的命令和技能互不兼容

### 解决方案

**VibeSOP = SkillOS = 智能路由 + 任务编排 + 生命周期管理 + 生态集成**

1. **理解意图** → 自然语言输入
2. **智能匹配** → 10 层路由 pipeline
3. **学习偏好** → 越用越准确
4. **跨平台** → 不绑定任何工具

---

## 核心原则

### 原则 1: 意图优先

**Don't guess, understand.**

**解读**: 不要让用户猜测命令，要理解用户的真实意图。

**实践**:
- 10 层路由 pipeline，逐层理解
- AI 语义分析（Layer 0）
- 场景模式匹配（Layer 2）
- 多维度相似度计算（Layer 3-6）

### 原则 2: 数据驱动

**Trust data, not intuition.**

**解读**: 相信数据，不依赖直觉。

**实践**:
- InstinctLearner: 学习用户偏好
- MemoryManager: 记住有效选择
- 反馈收集: 持续优化准确率

### 原则 3: 渐进增强

**Start simple, iterate rapidly.**

**解读**: 从简单开始，快速迭代。

**实践**:
- v4.0: 基础路由（85% 准确率）
- v4.1: 优化权重（94% 准确率）
- 未来: 持续优化到 95%+

### 原则 4: 开放生态

**Be open, not closed.**

**解读**: 保持开放，不锁定。

**实践**:
- SKILL.md 标准规范
- 多平台适配器支持
- 外部技能包集成
- 开放的反馈机制

---

## 设计哲学

### 简单性

**Less is more.**

- **用户端**: 一个命令解决所有问题
- **开发端**: 清晰的 SKILL.md 格式
- **架构**: 三层解耦，易扩展

**避免**:
- 过度的配置
- 复杂的命令行
- 晦能的默认值

### 透明性

**Show your work.**

- **开源代码**: 所有算法可见
- **清晰文档**: 解释决策原因
- **可验证**: 基准测试数据

**实践**:
- 架构决策记录（ADR）
- 优化报告（前后对比）
- 性能基准测试

### 可扩展性

**Design for evolution.**

- **接口化**: SKILL.md 标准化
- **模块化**: 三层架构
- **可插拔**: 匹配器协议

**实践**:
- 新技能: 创建 SKILL.md
- 新平台: 实现适配器
- 新匹配器: 实现 IMatcher

---

## 我们反对什么

### 反对 1: 记忆负担

**问题**: "记住这 45 个命令"

**反对理由**:
- 违反人类认知规律
- 增加学习成本
- 容易出错

**我们的方案**:
- 自然语言输入
- 智能路由匹配
- 自动学习偏好

### 反对 2: 平台锁定

**问题**: "只用 Claude Code"

**反对理由**:
- 限制用户选择
- 被平台生态绑架
- 无法享受其他工具的优势

**我们的方案**:
- 跨平台通用
- 标准化技能格式
- 可插拔适配器

### 反对 3: 手动搜索

**问题**: "去查文档找命令"

**反对理由**:
- 低效
- 中断心流
- 容易遗漏

**我们的方案**:
- 智能推荐
- 一键路由
- 自动学习

### 反对 4: 暴力优化

**问题**: "无限制地增加新特性"

**反对理由**:
- 功能臃肿
- 难以维护
- 用户困惑

**我们的方案**:
- 清晰的优先级
- 数据驱动决策
- 渐进式迭代

---

## 我们追求什么

### 目标 1: 95% 准确率

**当前**: 94%
**目标**: 95%
**方法**: 反馈驱动优化

### 目标 2: <100ms 响应时间

**当前**: ~270ms (复杂路由)
**目标**: <100ms
**方法**: 缓存优化、算法改进

### 目标 3: 支持 100+ 技能

**当前**: 45+ 技能
**目标**: 100+ 技能
**方法**: 生态扩展

### 目标 4: 支持 10+ 平台

**当前**: 3+ 平台 (Claude Code, Cursor, Continue)
**目标**: 10+ 平台
**方法**: 适配器生态

---

## 我们如何工作

### 开发原则

1. **用户至上**: 每个决策都从用户视角出发
2. **数据驱动**: 用数据验证假设，用数据指导优化
3. **快速迭代**: 小步快跑，持续改进
4. **质量优先**: 不牺牲质量追求速度

### 技术原则

1. **简单优于复杂**: 能简单的决不复杂
2. **标准优于定制**: 能标准化的决不定制
3. **开放优于封闭**: 能开放的决不封闭
4. **渐进优于激进**: 能渐进的决不激进

### 协作原则

1. **尊重原创**: 致敬并引用开源项目
2. **社区优先**: 社区需求优于技术兴趣
3. **透明公开**: 开源代码，开放决策
4. **谦逊学习**: 承认不足，持续学习

---

## 我们的愿景

### 短期 (6 个月)

**成为 AI 辅助开发的标准技能操作系统（SkillOS）**

- 95%+ 准确率
- <100ms 响应时间
- 支持 100+ 技能
- 覆盖主流平台

### 中期 (1-2 年)

**建立 AI 辅助开发的技能生态**

- 标准化的技能格式
- 繁荣的技能市场
- 多平台的技能共享
- 社区驱动的创新

### 长期 (3+ 年)

**重新定义 AI 辅助开发的工作方式**

- 意图理解成为标准
- 技能组合成为常态
- 跨平台协作无缝
- 人机协同高效

---

## 我们的承诺

### 对用户

- ✅ **准确**: 95%+ 的路由准确率
- ✅ **快速**: <100ms 的响应时间
- ✅ **可靠**: 持续可用的服务
- ✅ **透明**: 开源的代码和文档
- ✅ **开放**: 不锁定任何平台

### 对开发者

- ✅ **简单**: 易于扩展的架构
- ✅ **清晰**: 完整的文档和示例
- ✅ **标准**: 统一的技能格式
- ✅ **支持**: 活跃的社区

### 对社区

- ✅ **开源**: 永久开源（MIT 许可）
- ✅ **包容**: 欢迎所有贡献者
- ✅ **尊重**: 认可和引用原创
- ✅ **透明**: 公开的决策过程

---

## 核心价值观

### **Discovery over Execution** (发现 > 执行)

**含义**: 找到正确的工具比执行更重要

**体现**: 智能路由，而非技能执行

### **Matching over Guessing** (匹配 > 猜测)

**含义**: 理解意图比猜测命令更重要

**体现**: 10 层路由，而非简单关键词匹配

### **Memory over Intelligence** (记忆 > 智能)

**含义**: 记住有效选择比"更聪明"更重要

**体现**: InstinctLearner，学习用户偏好

### **Open over Closed** (开放 > 封闭)

**含义**: 开放生态比封闭系统更有价值

**体现**: SKILL.md 标准，跨平台支持

---

## 我们不做什么

### 我们是什么

| ✅ | 说明 |
|----|------|
| **SkillOS（技能操作系统）** | 技能的全生命周期管理层 |
| 智能路由器 | 理解意图，匹配最佳技能 |
| 任务编排器 | 分解复杂请求，规划执行顺序 |
| 技能生命周期管理器 | 启禁用、作用域隔离、质量评估、自动淘汰 |
| 跨平台适配层 | 一套技能定义，所有 AI Agent 通用 |
| 开放生态 | 任何人可以发布、分享、安装技能 |

### 我们不是什么

| ❌ | 说明 |
|----|------|
| 技能执行器 | 执行技能代码的是 AI Agent，不是 VibeSOP |
| AI 编码工具 | VibeSOP 管理技能，AI Agent 执行技能 |
| 单平台工具 | 不绑定 Claude Code、Cursor 或任何特定平台 |
| 闭源系统 | 永久开源（MIT），开放决策过程 |
| **技能生产者** | **VibeSOP 管理技能的分发和生命周期，但不编写、不修改技能的具体执行流程** |

> **技能内容边界**: VibeSOP 不生产技能内容。每个技能的 SKILL.md 正文由其作者编写和维护（如 gstack、superpowers、omx 或社区作者）。VibeSOP 在分发时只追加元数据 frontmatter（如 namespace、version、installed_at），绝不修改或替换技能正文。Agent 读取到的是技能的原始完整内容，不是 VibeSOP 生成的薄包装。
>
> **执行能力说明**: VibeSOP 保留轻量级本地执行能力（`ExternalSkillExecutor`、AST 安全评估器）用于开发者测试和技能验证。但在生产流程中，技能的最终执行由 AI Agent（Claude Code、OpenCode、Cursor 等）完成。这是有意的边界：SkillOS 管"用什么技能、以什么顺序"，AI Agent 管"怎么执行"。

> **定位边界**: VibeSOP 是 SkillOS，管理技能的**全生命周期**：
> **发现 → 安装 → 路由 → 编排 → [分发: 完整保留技能内容] → 评估 → 保留/淘汰**
>
> 其中"执行"环节由 AI Agent（Claude Code、OpenCode、Cursor 等）完成。
> 这是有意的架构分层：SkillOS 管"用什么技能、以什么顺序"，
> AI Agent 管"怎么执行"。两者解耦，确保平台无关性。
>
> **分发原则**: VibeSOP 同步技能到平台目录时，采用以下策略：
> 1. **外部技能包**（gstack、superpowers、omx 等）：完整保留原始内容，使用 symlink 或完整复制，不修改 SKILL.md 正文
> 2. **内置管理工具**（slash-route 等）：仅包含元数据和命令说明，不含具体执行流程
> 3. **通用兜底工作流**（riper-workflow）：作为无具体技能匹配时的 fallback，保留完整流程

### VibeSOP 内置技能清单

VibeSOP 自身提供**管理工具**、**通用兜底工作流**和**系统级工作流技能**。

| 技能 | 类型 | 说明 |
|------|------|------|
| slash-route | 管理工具 | 路由查询：理解意图，匹配最佳技能 |
| slash-help | 管理工具 | 系统帮助：展示可用命令和用法 |
| slash-install | 管理工具 | 安装技能包：从市场或 URL 安装 |
| slash-list | 管理工具 | 列出技能：查看已安装和可用技能 |
| slash-evaluate | 管理工具 | 评估技能质量：质量评分和反馈 |
| slash-orchestrate | 管理工具 | 多技能编排：分解复杂请求为执行计划 |
| riper-workflow | 通用兜底 | 无具体技能匹配时的 fallback 工作流 |
| instinct | 系统工作流 | 记录和演化使用模式为可复用本能 |
| instinct-learning | 系统工作流 | 自动从会话历史中提取模式 |
| experience-evolution | 系统工作流 | 项目知识积累与经验沉淀 |
| session-end | 系统工作流 | 会话收尾：更新 handoff + 提交 + 记录经验 |
| autonomous-experiment | 系统工作流 | 自主实验循环（预测-归因-评估） |
| skill-craft | 系统工作流 | 从会话历史生成个人技能 |

> **原则**: 所有**具体领域**的技能（如代码审查、调试、架构设计、数据分析）应由外部技能包（gstack、superpowers、omx 等）或社区提供。VibeSOP 内置的技能限于系统管理工具和 VibeSOP 自身工作流所需的基础技能。

### 上下文感知 & 自主学习

VibeSOP 内置的上下文感知和自主学习功能**默认自动开启**，无需手动配置。

| 功能 | 状态 | 工作原理 |
|------|------|---------|
| **Session 智能追踪** | ✅ 自动 | 每次路由后自动记录当前技能会话状态，session-aware 默认开启 |
| **上下文感知重路由** | ✅ 自动 | 检测到意图转换（如从调试切换到设计）时，在 `RoutingResult.metadata` 中嵌入 `reroute_suggestion`，Agent 需显式检查 |
| **自主学习 (InstinctLearner)** | ✅ 自动 | 置信度 ≥ 70% 的路由决策自动记录为 instinct，为后续路由提供偏好 boost |

#### 配置关闭

如需关闭上下文感知，在配置文件（`.vibe/config.toml` 或 `~/.vibe/config.toml`）中设置：

```toml
[routing]
session_aware = false
```

或在 CLI 中：

```bash
vibe config set routing.session_aware false
```

> **注意**: 关闭 `session_aware` 会同时禁用 Session 追踪和上下文感知重路由。InstinctLearner 的自动学习仍会运行（基于路由置信度），但不附带会话上下文。

### 配置与数据目录

VibeSOP 使用以下目录存储配置和数据。所有路径遵循 XDG 规范：

| 路径 | 用途 | 级别 |
|------|------|------|
| `.vibe/config.toml` | 项目级路由、编排、技能配置 | 项目 |
| `.vibe/skills/` | 项目级自定义技能存储 | 项目 |
| `.vibe/preferences.json` | 偏好学习数据（自动维护） | 项目 |
| `.vibe/session/` | 会话状态持久化（自动维护） | 项目 |
| `~/.vibe/config.toml` | 全局默认配置 | 用户 |
| `~/.config/skills/` | 外部技能包中央存储（gstack/superpowers/omx） | 用户 |
| `~/.claude/`, `~/.config/opencode/`, `~/.kimi/` | 各平台配置文件目录 | 用户 |

**优先级**: 环境变量 > 项目配置 > 全局配置 > 默认值

### Agent Runtime 说明

VibeSOP 支持两种 LLM 集成模式：

| 模式 | 说明 | API Key |
|------|------|---------|
| **进程内集成** | Agent 代码直接调用 `AgentRouter.set_llm()`，复用 Agent 的 LLM 客户端 | 无需单独配置 |
| **CLI 子进程** | `vibe route` 作为独立进程运行（route hook 的实际场景） | 需配置 `VIBESOP_LLM_PROVIDER` 环境变量或 `.vibe/config.toml` 中的 `llm` 字段 |

> CLI 子进程模式是当前大多数平台的默认运行方式（通过 hook 调用），因此需要为 VibeSOP 单独配置 LLM 凭证。详见 [SKILL_LLM_CONFIG_GUIDE.md](SKILL_LLM_CONFIG_GUIDE.md)。

### 我们不追求

| ❌ | ✅ |
|----|----|
| 100% 准确率 | 95% 准确率（实用） |
| 0ms 延迟 | <100ms 延迟（够快） |
| 所有技能 | 100+ 主流技能（够用） |
| 所有平台 | 10+ 主流平台（覆盖） |
| 无限扩展 | 有序演进（可持续） |

---

## 总结

**VibeSOP 的核心哲学**:

> **让 AI 辅助开发变得像对话一样自然。**

**不再记忆命令，只需表达意图。**

**不再猜测工具，智能匹配最佳。**

**不再学习平台，一次掌握所有。**

**技能全生命周期，从发现到淘汰，VibeSOP 全程管理。**

---

**这，就是我们的信仰。**

---

**状态**: ✅ 哲学文档完成
