# 验证器与计划交付合同（Verification & Plan-Delivery Contract）

> **文档版本**：2026-09-09（对齐 8.3.0.dev1）
> **状态**：合同已定稿；A（计划生成）/B（内置验收技能）/C（正文拒绝统一）/D（版本与集成）
> 四路并行实现中。本文描述合同目标语义；实现落地前用户可见行为以各路由合流后的 8.3.0 为准。
> 上游：`docs/specs/2026-09-09-verification-contract.md`（主控维护）。

## 1. 背景与范围

默认对抗计划应能使用随包提供的验收技能，不要求安装 gstack。验证必须围绕**用户原始要求与
实际产物**——代码、文档、分析、部署任务均可使用验收；不能把通用验收偷偷换成代码评审。
任一步骤的技能正文被现有扫描器拒绝时，所有计划交付入口都必须阻断，不能继续宣称可执行。

本合同的验收结论只回答「产物是否满足用户原始要求」，不代替执行、不修改实现、不标记计划完成。

## 2. 显式选择与默认值

核心 API 约定：

```python
PlanBuilder.build_plan(
    original_query,
    sub_tasks,
    workflow_pattern=...,
    verifier_skill_id="builtin/verify-result",  # 可选关键字，缺省使用随包内置验收技能
)
```

- 缺省（不传参）等价于显式 `"builtin/verify-result"`；参数默认值必须与显式无效输入可区分
  （传 `None`/空串不是「用默认」，而是无效输入）。
- 显式指定**非空**技能 ID 时使用指定值（例如用户/编排点名的其他验收技能）。
- 空白、非字符串、`fallback-llm` 一律拒绝，不能静默回退默认。
- 不增加模型调用，不读取环境变量决定验证器，不增加全局自动注入。
- 参数只作用于 `ADVERSARIAL`；其他工作流（SEQUENTIAL / FAN_OUT / TOURNAMENT /
  LOOP_UNTIL_DRY / 小队类）行为不变。

内置技能 `builtin/verify-result` 仅在显式选择或编排选择时被使用；`disable-model-invocation:
true`，无宽泛自然语言触发器（避免被路由误自动拉起）。

## 3. 验证步骤结构

ADVERSARIAL 计划末尾追加的验证步骤必须：

- **依赖所有原步骤**（等全部原步骤完成后才执行），保留 `is_verification_step=True`、
  `QUARANTINE`、`pending` 与原始请求。
- 输入向验证技能提供：各步骤的**任务类型**（沿用项目现有任务/步骤分类词，不引入
  新枚举）、**预期产出**与**原始需求**，要求逐项证据。
- 不能默认通过，不能自行执行未获授权的部署。
- 验证围绕用户原始要求和实际产物展开；产物可以是代码、文档、分析、部署状态。

## 4. 按任务类型验收

| 任务类型 | 检查方法（正文要求） |
| --- | --- |
| 代码 | 测试与行为：实际测试/运行记录证明行为符合要求，而非只看 diff 形状 |
| 文档 | 完整性与来源：覆盖原始要求各要点，来源/出处可核 |
| 分析 | 数据和计算：原始数据、计算过程与结论一致，数字可复算 |
| 部署 | 已有授权下的实际状态：查真实环境状态，不自行触发新的部署 |

## 5. 验收结论与证据边界

- 缺证据标 **blocked**；实际失败标 **failed**；只有清单全部满足才标 **passed**。
- 模型评审与机器验收**分别记录**：机器记录（执行日志、退出码、时间、产物指纹等）是证据；
  模型/代理的自我陈述不是证据。
- 不能以代理自述代替证据；不自动调用其他代理；不改实现、不运行未获授权部署、不标记计划完成。

## 6. 缺失与 unsafe 阻断

- 步骤技能文件缺失、正文为空，或正文被运行时扫描器判不安全（含扫描器自身失败，fail-closed），
  均视为该步骤不可交付。
- 任一步骤不可交付 → 整份计划不可执行：`metadata.execution_ready=False`；
  `blocked_steps` 给出阻断原因。可用性类原因沿用现状、不细分：技能缺失与正文为空
  共用 `not found or empty`，fallback/无技能为 `unresolved skill`；**unsafe** 是独立
  原因，必须能与可用性原因区分，但不能声称缺技能/空内容已各自细分。同时保留全部
  原步骤、验证标志与来源（供诊断，不当作可执行内容）。
- 各交付入口一致阻断：
  - guide 只输出阻断说明、**无完成标记**（guide 没有 `has_match` 字段）；
    hook / CLI 为 notice-only 且 `has_match=false`；
  - manifest 抛出诊断异常——**不能把拒绝提示当成技能正文继续生成可执行 manifest**；
  - manifest 实际读入正文后仍复核（注解与读取之间发生变更也不漏检），提示不回显危险正文。
- 保留既有语义：安全正文、单技能、来源优先级、空文件语义均不因本合同回退。

## 7. 交付合同测试

`tests/integration/test_verification_delivery_contract.py` 覆盖三个场景，全部基于真实模型、
临时文件与实际内置技能，不 mock 待验收接口：

1. 默认对抗计划可交付（默认验证器为 `builtin/verify-result`，文件与正文均安全时可交付）；
2. 显式指定缺失验证器 → 阻断（不能静默回退默认）；
3. 安全正文被改成不安全正文 → 拒绝（`execution_ready=False`，原因含 unsafe，manifest 抛异常）。

A/B/C 未集成前本测试按合同预期失败并保留结果，等待各路合流后转绿。
