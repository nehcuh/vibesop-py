# {{ skill.name }}

{% if skill.description %}
> {{ skill.description }}
{% endif %}

**Skill ID**: `{{ skill.id }}`
**Namespace**: {{ skill.namespace or 'builtin' }}
**Version**: {{ skill.version or '1.0.0' }}
{% if skill.author %}
**Author**: {{ skill.author }}
{% endif %}

## When to Use

{{ skill.trigger_when }}

{% if skill.tags %}
**Tags**: {{ skill.tags | join(', ') }}
{% endif %}

## Skill Type

{% if skill.skill_type == 'prompt' %}
**Type**: Prompt-Based Skill

This skill uses LLM prompt templates to execute tasks.
{% elif skill.skill_type == 'workflow' %}
**Type**: Workflow Skill

This skill executes a multi-step workflow.
{% elif skill.skill_type == 'command' %}
**Type**: Command Skill

This skill runs shell commands.
{% else %}
**Type**: Hybrid Skill

This skill combines multiple execution types.
{% endif %}

## Metadata

{% if skill.metadata %}
{% for key, value in skill.metadata.items() %}
**{{ key }}**: {{ value }}

{% endfor %}
{% else %}
No additional metadata available.
{% endif %}

## Usage

### Via AI Routing

```
vibe route "<request matching this skill>"
```

### Direct Invocation

```
vibe skills use {{ skill.id }}
```

## Implementation Details

This skill was generated by VibeSOP as part of the configuration manifest.

**Generated**: {{ metadata.created_at.strftime('%Y-%m-%d %H:%M:%S') }}
**Manifest Version**: {{ metadata.version }}

---

*Part of VibeSOP skill registry*
