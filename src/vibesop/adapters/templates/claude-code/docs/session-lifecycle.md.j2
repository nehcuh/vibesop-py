# Session Lifecycle

> **On-Demand**: Loaded for session management events

## Session End

When the user signals the end of a session, you MUST run `session-end`.

### English Signals

"that's all for now", "heading out", "I'm leaving", "I'm done", "gotta go", "wrap it up", "call it a day"

### Chinese Signals

"我要离开了", "先走了", "拜拜", "再见", "今天就到这里", "就到这里吧", "收工"

### Explicit Command

`/session-end`

### Fallback

If routing fails (short-query bypass, no confident match):
- Do NOT skip session-end because `vibe route` returned fallback-llm
- Run `vibe route --slash "/session-end"` and read the printed `skill_file` / `NEXT STEP` path. Do not guess `skills/session-end/SKILL.md`.
- This is a P0 mandatory skill — skipping it is a protocol violation

---

*Part of VibeSOP session management*
