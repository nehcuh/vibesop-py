# Project Context

> **Project-level AGENTS.md** — highest priority in Pi Coding Agent

{% if tool_environment %}
{{ tool_environment }}

---
{% endif %}

## VibeSOP Routing

Routing is automatic when the VibeSOP Pi extension is installed. If the
current user prompt arrives with an extension injection — look for
`VibeSOP Routing Result` or `Matched skill:` — routing has already run
for this prompt: follow that result and do NOT re-run `vibe route`.

If no such injection is present on the current prompt (extension not
installed or no skill matched), run:

    vibe route "<user_request>"

then read the SKILL.md path from the routing result (`skill_file` in JSON, or the `SKILL.md:` / `NEXT STEP` line) and follow its steps. Do not guess `.pi/skills/<id>/SKILL.md` — the generated tree there may be empty.

For override protocol: read `.pi/docs/routing-protocol.md`.

## Project-Specific Context

<!-- Add project conventions below. Examples:

### Tech Stack

### Architecture

### Coding Standards

### Testing

### Deployment

-->
