#!/bin/bash
# Post-session-start hook for {{ platform }}
# This hook runs after the session starts
#
# Generated by VibeSOP - DO NOT EDIT MANUALLY

set -e

echo "[{{ hook_point }}] Session started for {{ platform }}"

# Initialize session
if command -v vibe &> /dev/null; then
    echo "[{{ hook_point }}] VibeSOP CLI detected"
    # TODO: Implement when vibe session init is available
    # vibe session init
else
    echo "[{{ hook_point }}] VibeSOP CLI not found"
fi

# Custom hook logic can be added here
{% if platform == "claude-code" %}
# Claude Code specific initialization
echo "[{{ hook_point }}] Claude Code environment ready"
{% elif platform == "opencode" %}
# OpenCode specific initialization
echo "[{{ hook_point }}] OpenCode environment ready"
{% endif %}

exit 0
